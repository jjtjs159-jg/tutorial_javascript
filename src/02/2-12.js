let first = null;
let last = null;
let nick = 'supercoder';

// null 병합 연산자, 폴리필 주의
// alert(first ?? last ?? nick ?? 'anonymouse');

let height = 0;

// alert(height || 100); // 100
// alert(height ?? 100); // 0

// let x = 1 && 2 ?? 3; error